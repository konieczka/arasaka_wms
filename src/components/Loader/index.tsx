import React from "react";
import { LoaderWrapper, Loading } from "./styles";

const Loader = () => (
  <LoaderWrapper>
    <Loading>Loading...</Loading>
  </LoaderWrapper>
);

export default Loader;
